CREATE OR REPLACE FUNCTION oterEmployeDesProjetsDeCategorie (i_emp_nom IN exa_employe.emp_nom_prenom%TYPE, i_cat_nom IN exa_categorie.cat_nom%TYPE) RETURN VARCHAR2  IS
  FunctionResult VARCHAR2(200);
BEGIN
  
  RETURN (FunctionResult);
END;
/
